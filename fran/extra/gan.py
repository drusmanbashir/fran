"""Legacy GAN scratchpad module.

Import-safe placeholder: the original script executed dataset loading at import time.
"""


def note() -> str:
    return "Run GAN experiments via dedicated scripts, not module import side effects."


if __name__ == "__main__":
    print(note())
