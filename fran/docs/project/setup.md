## 1.Update projects

Scenarios when a project needs updating:    

### i)You added new data to the folder

Simplye re-add the DataSpec by running:

`P.add_data([DS.nodes])`

any new cases added to the folder will update the project automatically

