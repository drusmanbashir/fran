mamba install -c conda-forge ipython ipywidgets pandas matplotlib medpy numpy openpyxl pillow pygments jupyterlab pyyaml scikit-image scipy SimpleITK timm torchio tqdm einops monai ipdb gputil ray-tune ipympl lxml seaborn tensorboard
mamba install -c pytorch ignite
pip install nnunet neptune pipe connected-components-3d h5py torchinfo
mamba install -c fastchan fastai torchinfo

# -----------others --------------------
pip install ipympl ipdb
mamba install -c radiomics pyradiomics
mamba install -c conda-forge black
#note some of these will downgrade your pytorch copy so re-install it after


# build from source: 
# surface-distance
#pytorch, torchvision, confirm version remains intact AFTER running above installs and esp after fastai installation 

# note: h5py installation is incompatible with monai inside slicer
Inside slicer:
import pip
mods="lightning","monai","ipdb" "lxml","seaborn","nnunet","bs4", "ipywidgets",
modules= "torch", "torchvision","lightning","monai", "fastcore","fastai", ipython","ipywidgets","pandas","matplotlib","openpyxl","pillow","pygments","jupyterlab","pyyaml","scikit-image","scipy","timm","einops","ipdb","gputil","ipympl","lxml","seaborn","nnunet","pyxnat", "paramiko","itk", "neptune"
def install(modules):
    for mod in modules:
      pip.main(['install',mod])

install(modules)
install(mods)

